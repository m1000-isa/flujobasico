# flujobasico
# Practica 1-Flujo Básico de Git y GitHub
  ```sh
   git status
     git add .
     git status
     git commit -m "Descripcion del cambio que hiciste en el archivo README.md"
     git push
  ```
